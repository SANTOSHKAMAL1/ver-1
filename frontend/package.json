{
  "name": "frontend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/main.jsx --bundle --minify --loader:.js=jsx --loader:.jsx=jsx --define:process.env.NODE_ENV=\\\"production\\\" --outfile=../static/assets/bundle.js",
    "watch": "esbuild src/main.jsx --bundle --loader:.js=jsx --loader:.jsx=jsx --define:process.env.NODE_ENV=\\\"development\\\" --outfile=../static/assets/bundle.js --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "jsdom": "^30.0.1"
  }
}
